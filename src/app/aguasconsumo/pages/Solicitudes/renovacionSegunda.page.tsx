import { RenovarS } from 'app/aguasconsumo/Components/RenovarS';

import Tabs from 'antd/es/tabs';

import React, { useCallback, useEffect, useState } from 'react';

// Otros componentes

const RenovarSegundaVez: React.FC<any> = (props: any) => {
  const { TabPane } = Tabs;

  return <RenovarS />;
};

export default RenovarSegundaVez;
