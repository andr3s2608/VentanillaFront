import { NotificarB } from 'app/aguasconsumo/Components/NotificarB';

import Tabs from 'antd/es/tabs';

import React, { useCallback, useEffect, useState } from 'react';

// Otros componentes

const NotificarBandeja: React.FC<any> = (props: any) => {
  const { TabPane } = Tabs;

  return <NotificarB />;
};

export default NotificarBandeja;
