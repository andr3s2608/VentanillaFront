import { Aprobar } from 'app/aguasconsumo/Components/Aprobar';

import Tabs from 'antd/es/tabs';

import React, { useCallback, useEffect, useState } from 'react';

// Otros componentes

const AprobarTramite: React.FC<any> = (props: any) => {
  const { TabPane } = Tabs;

  return <Aprobar />;
};

export default AprobarTramite;
